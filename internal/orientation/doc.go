// Package orientation owns deterministic derivation that spans domain records.
//
// It may depend on the domain package, but it must remain independent from
// persistence, network, provider, CLI, and other infrastructure concerns.
package orientation
